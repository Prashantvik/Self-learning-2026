#### SQL Tips and tricks for problems solving

- Query execution order 
- Self Joins
- Subqueries
- EXISTS vs IN
- Correlated subqueries
- Pivot vs Unpivot
- Conditional aggregation (pivot example)
- Window functions (ROW_NUMBER, RANK, DENSE_RANK, LAG, LEAD etc)
- Common table expressions (CTEs)
- Recursive CTEs for hierarchical data
- Advanced window functions (FIRST_VALUE, LAST_VALUE, NTH_VALUE, PERCENTILE_CONT)


- Query optimization techniques and execution plans
- Index strategies (clustered vs non-clustered, covering indexes, partial indexes)
- Partitioning strategies (range, hash, list partitioning)
- Performance tuning (query hints, statistics, cardinality estimation)
- Data warehouse modeling (star schema, snowflake schema, slowly changing dimensions)
- SQL performance patterns and anti-patterns
- Advanced joins (lateral joins, cross apply, outer apply)
- Set operations (UNION, INTERSECT, EXCEPT) with optimization
- JSON/XML data handling in SQL
- Temporal tables and time-based queries
- Stored procedures, functions, and triggers optimization
- Dynamic SQL and parameterized queries
- Bulk operations (MERGE, BULK INSERT, batch processing)
- Database-specific optimizations (PostgreSQL, SQL Server, Oracle, etc.)
- Data quality checks and constraint management
- SQL for ETL/ELT processes and data pipelines
- Advanced analytical functions (statistical functions, regression)
- Graph queries and hierarchical data processing
- Memory-optimized tables and in-memory processing
- Columnstore indexes and analytical workloads
- Database sharding and distributed query patterns
- SQL security patterns (row-level security, column masking)
- CDC (Change Data Capture) and streaming data patterns 